---
wave: 4
depends_on: [2, 3]
files_modified: []
autonomous: false
requirements: [OUT-03, LOAD-01, LOAD-02, VERIFY-01]
---

# Plan 04-04: 端到端 UAT 验证

**Objective:** 在 BP_FirstPersonCharacter.uasset 上运行完整 CLI 流程，验证 Phase 4 的 3 个成功标准全部通过。

## Task 4.1: UAT 1 — 单条 CLI 命令 + 格式选择

<read_first>
- scripts/main.py
</read_first>

<acceptance_criteria>
- `python scripts/main.py BP_FirstPersonCharacter.uasset --format json` 退出码 0，输出 JSON 文件
- `python scripts/main.py BP_FirstPersonCharacter.uasset --format md` 退出码 0，输出 MD 文件
- `python scripts/main.py --help` 显示完整帮助信息，退出码 0
- `python scripts/main.py --version` 显示 "uasset_resolver 0.1.0"，退出码 0
- JSON 输出包含 nodes/pins/connections 字段
- MD 输出包含 Begin Object ... End Object 块
</acceptance_criteria>

<action>
手动执行上述命令，验证退出码和输出内容。确认格式选择正常工作。
</action>

## Task 4.2: UAT 2 — 裸文件加载 + 错误提示

<read_first>
- scripts/main.py
- scripts/controller.py
</read_first>

<acceptance_criteria>
- `python scripts/main.py nonexistent.uasset` 退出码 1，错误信息包含 "文件不存在"
- `python scripts/main.py BP_FirstPersonCharacter.uasset --backend cue4parse` 成功（无需 Content 目录）
- 若 CUE4Parse 不可用且 UE headless 也不可用：错误信息包含三个选项（编译 CUE4Parse / --content-dir / --ue-path）
</acceptance_criteria>

<action>
执行上述命令验证错误处理和回退提示。
</action>

## Task 4.3: UAT 3 — 交叉验证通过

<read_first>
- scripts/verify.py
- scripts/main.py
- 蓝图节点文本参考.md
</read_first>

<acceptance_criteria>
- `python scripts/main.py BP_FirstPersonCharacter.uasset --format json --verify` 退出码 0
- 验证报告包含：
  - 节点数量：PASS 或 PARTIAL（.usmap 缺失时可能少提取）
  - 节点名称匹配率：>= 80%
  - 函数引用：至少 3 个函数名匹配
  - 引脚结构：PASS 或 PARTIAL
- 总体结果：至少 3/4 维度 PASS
</acceptance_criteria>

<action>
执行带 --verify 的命令，验证交叉验证逻辑。
</action>
